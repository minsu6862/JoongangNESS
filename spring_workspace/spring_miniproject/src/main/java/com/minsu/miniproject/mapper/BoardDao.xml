<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.minsu.miniproject.dao.BoardDao">
	<!-- board:member 1:1관계 매핑 -->
	<resultMap type="com.minsu.miniproject.dto.BoardDto" id="boardWithMember">
		<id property="bnum" column="bnum" /> <!-- boardtbl 테이블의 기본키 셋팅 -->
		<result property="btitle"  column="btitle" /> <!-- boardtbl 테이블의 나머지 필드 셋팅 -->
		<result property="bcontent"  column="bcontent" />
		<result property="bwriter"  column="bwriter" />
		<result property="bhit"  column="bhit" />
		<result property="bdate"  column="bdate" />
		<result property="rnum"  column="rnum" />
		
		<association property="memberDto" javaType="com.minsu.miniproject.dto.MemberDto">
			<id property="memberid" column="memberid" />
			<result property="membernum" column="membernum" />
			<result property="memberpw" column="memberpw" />
			<result property="membername" column="membername" />
			<result property="memberdate" column="memberdate" />
		</association>
	</resultMap>
	
	<!-- 모든 글 갯수 가져오기 -->
	<select id="allBoardCount" resultType="int">	
		SELECT COUNT(*) FROM boardtbl
	</select>
	
	<!-- 페이징 적용시킨 리스트 호출, &lt; &gt;를 사용해도 되긴함 -->
	<select id="pageBoardList" resultMap="boardWithMember">
	<![CDATA[
		select *
    		from (select ROWNUM as rnum, r.*
            	from
                	(SELECT b.bnum, b.btitle, b.bcontent, b.bwriter, m.membername, b.bhit, b.bdate
                    	FROM boardtbl b
                        	INNER JOIN membertbl m ON b.bwriter = m.memberid	
                            	ORDER BY b.bnum DESC) r WHERE ROWNUM <= #{param2})
                                	WHERE rnum >= #{param1}
	]]>
	</select>
	
	<insert id="boardWrite">
		INSERT INTO boardtbl(bnum, btitle, bcontent, bwriter, bhit)
			VALUES(board_seq.nextval, #{param1}, #{param2}, #{param3}, 1)
	</insert>
	
	<update id="updateHit">
		UPDATE boardtbl SET bhit=bhit+1 WHERE bnum=#{param1}
	</update>
	
	<select id="contentView" resultMap="boardWithMember">
		SELECT b.bnum, b.btitle, b.bcontent, b.bwriter, m.membername, b.bhit, b.bdate
			FROM boardtbl b
				INNER JOIN membertbl m ON b.bwriter = m.memberid	
				   WHERE b.bnum = #{param1}				
	</select>
	
	<update id="boardModify">
		UPDATE boardtbl SET btitle=#{param2}, bcontent=#{param3} WHERE bnum=#{param1}
	</update>
	
	<delete id="boardDelete">
		DELETE FROM boardtbl WHERE bnum=#{param1}
	</delete>
</mapper>