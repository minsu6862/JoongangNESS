<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.minsu.miniproject.dao.MemberDao">
	
	<insert id="memberJoin">
		INSERT INTO membertbl(membernum, memberid, memberpw, membername)
			VALUES(member_seq.nextval , #{param1}, #{param2}, #{param3})
	</insert>
	
	<select id="memberIdCheck" resultType="int">
		SELECT COUNT(*) FROM membertbl WHERE memberid = #{param1} 
		<!-- 아이디가 존재시 1이 반환 -->
	</select>
	
	<select id="memberLogin" resultType="int">
		SELECT COUNT(*) FROM membertbl WHERE memberid = #{param1} AND memberpw = #{param2} 
		<!-- 아이디가 존재시 1이 반환 -->
	</select>
	
	<select id="getMemberName" parameterType="String" resultType="String">
    	SELECT membername FROM membertbl WHERE memberid = #{memberId}
	</select>
	
</mapper>