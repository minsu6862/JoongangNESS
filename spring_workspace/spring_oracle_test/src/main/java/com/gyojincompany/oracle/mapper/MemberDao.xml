<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.gyojincompany.oracle.dao.MemberDao">
	
	<insert id="memberjoinDao">
		INSERT INTO membertbl(membernum, memberid, memberpw, membername)
			VALUES(member_seq.nextval , #{param1}, #{param2}, #{param3})
	</insert>
	
	<select id="memeberidCheckDao" resultType="int">
		SELECT COUNT(*) FROM membertbl WHERE memberid = #{param1} 
		<!-- 아이디가 존재시 1이 반환 -->
	</select>
	
	<select id="memberLoginDao" resultType="int">
		SELECT COUNT(*) FROM membertbl WHERE memberid = #{param1} AND memberpw = #{param2} 
		<!-- 아이디가 존재시 1이 반환 -->
	</select>
	
	
</mapper>
