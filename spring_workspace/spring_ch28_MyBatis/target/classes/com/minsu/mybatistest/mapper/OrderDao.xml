<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.minsu.mybatistest.dao.OrderDao">
	
	<select id="userOrderListDao" resultType="com.minsu.mybatistest.dto.UserOrderDto">
		SELECT u.userid, u.username, o.orderid, o.ordername FROM springdb.user u
		INNER JOIN springdb.order o ON u.user_id = o.user_id
		WHERE u.user_id = #{user_id}
	</select>
	
</mapper>