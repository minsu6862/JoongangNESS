<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.minsu.mybatistest.dao.IDao">

	<!-- <select id="com.minsu.mybatistest.dto.BoardDto">
		SELECT * FROM boardtbl ORDER BY board_num DESC
	</select> -->
	<!-- select문이 아닐 경우 resultType은 필요없음 -->
	<select id="boardlistDao" resultType="com.minsu.mybatistest.dto.BoardDto">
		SELECT * FROM boardtbl ORDER BY board_num DESC
	</select>
	
	<insert id="boardWriteDao">
		INSERT INTO boardtbl (board_title, board_content, board_name, board_hit)
		VALUES (#{board_title}, #{board_content}, #{board_name}, 0)
	</insert>
	
	<delete id="boardDeleteDao">
		DELETE FROM boardtbl WHERE board_num=#{board_num}
	</delete>
	
	<select id="contentViewDao" resultType="com.minsu.mybatistest.dto.BoardDto">
		SELECT * FROM boardtbl WHERE board_num=#{board_num}
	</select>
	
	<update id="boardUpdateDao">
		UPDATE boardtbl 
    	SET board_title = #{board_title}, board_content = #{board_content}, board_name = #{board_name} 
    	WHERE board_num = #{board_num}
	</update>
</mapper>